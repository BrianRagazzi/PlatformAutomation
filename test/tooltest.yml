jobs:
- name: test-tools
  plan:
  - task: check tools
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: ubuntu
      run:
        path: bash
        #args: [-c, "echo Hello, ((provided-by-credhub))"]
        args
